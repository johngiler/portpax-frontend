# LOCAL — copy to .env.local (not versioned)
# Used by: npm run dev

NEXT_PUBLIC_API_URL=http://localhost:8000
# Daphne (WebSockets). REST stays on :8000; omit to auto-use ws://localhost:8001.
NEXT_PUBLIC_WS_URL=http://localhost:8001
