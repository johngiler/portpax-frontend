# DEV / Testing (BCT) — copy to .env.dev (not versioned)
# Used by: scripts/deploy.sh (static export build for itm.portpax.com)

NEXT_PUBLIC_API_URL=https://api.portpax.com

# Same token as backend FRONTEND_BUILD_PUBLISH_TOKEN (Daphne app_update broadcast).
FRONTEND_BUILD_PUBLISH_TOKEN=
