# PRODUCTION (ITM) — copy to .env.production (not versioned)
# Used by: ITM production build (portpax.itmgroup.mx)

NEXT_PUBLIC_API_URL=https://api.itmgroup.mx

# Same token as backend FRONTEND_BUILD_PUBLISH_TOKEN (Daphne app_update broadcast).
FRONTEND_BUILD_PUBLISH_TOKEN=
